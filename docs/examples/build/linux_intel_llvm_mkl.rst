Linux + Intel LLVM + MKL
========================

Example build script for a Linux environment using the Intel LLVM compilers and MKL.
Optionally, the code can be built with LibRI support and/or Fortran binding.

.. literalinclude:: ../../../examples/build/linux_intel_llvm_mkl.sh
   :language: bash
